language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
# command to run tests
script:
  #- coverage run tests.py
  #- coverage report -m
  - coverage xml
  - export CODACY_PROJECT_TOKEN=%3a2bd2d4f11746da8d93632ed971dec6%
  - python-codacy-coverage -r coverage.xml

